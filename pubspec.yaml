name: currency_converter_flutter
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  http: ^0.13.4
  flutter_native_splash: ^1.3.1

flutter_native_splash:
  color: "#ffffff"
  color_dark: "#0d47a1"
  image: assets/Convert Icon.png
  image_dark: assets/Convert Icon_White.png
  android: true
  ios: true

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true
